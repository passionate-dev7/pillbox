You are the VIDEO agent for "Pill Round" at /Users/kamal/Desktop/devpost/projects/webmcp/pill-round. Deliver /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/out/pill-round-demo.mp4, under 180 s, 1920x1080, one voice, first tool call on camera by 0:12, then upload it to YouTube and record the URL in /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/docs/YOUTUBE.md.

Everything you need exists from the first entry. Copy the pipeline, do not rewrite it:
- Reference pipeline: /Users/kamal/Desktop/devpost/projects/webmcp/video/ (assemble/build_all.sh, build_segment.sh, make_captions.py, gen_pointer.py, cue_filter.py, cdp_record.mjs, timeline.tsv, cues/*.tsv, assets/pointer.png, slides/endcard.png; diagrams/exrender/{build_scenes.py,render.html,serve.py}). Read timeline.tsv and clips/clips.txt first to see the shape of a finished cut.
- Copy assemble/ and diagrams/exrender/ into /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/ (cp -R), then edit paths: build_all.sh OUT name to pill-round-demo.mp4; serve.py OUT to /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/diagrams; build_scenes.py palette to THIS repo's DESIGN.md tokens (read DESIGN.md at repo root) and content to THIS product.
- Narration is already generated: /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/narration/line1.wav .. line13.wav from /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/narration.txt (13 lines, female, young adult, seed 19). Do not regenerate. Measure each with ffprobe for audio_sec.

Beats (map narration lines to visuals; 13 lines):
1 problem.png (Dad 78, eleven prescriptions, four doctors, hand-written grid). 2 problem.png with ciprofloxacin + warfarin called out. 3 two-sessions.png (caregiver + pharmacist). 4 pipeline.png (openFDA labels, NDC, enforcement -> sentence index with set_id -> tools per role). 5 CLIP caregiver window: check_interactions executed from the page with {"add":"ciprofloxacin"}, TOOL LOG shows the call. 6 CLIP caregiver: Flags panel, warfarin row red, verbatim sentence and set_id link visible (scroll/zoom to it). 7 CLIP pharmacist window: tools panel showing 9 tools (propose_change, add_counsel_note present; accept_change absent); DevTools Application > WebMCP if possible. 8 CLIP pharmacist: propose_change hold warfarin + Confirm card. 9 CLIP caregiver window: proposal arrives with no reload, Accept button visible; accept_change from the page + Confirm; warfarin flips to HELD. 10 CLIP caregiver: report_side_effect declarative form, agent-filled, human presses Send. 11 CLIP caregiver: print_round_card + Confirm, round card grid on screen. 12 two-sessions.png. 13 hold on the Flags panel with the openFDA disclaimer footer visible; end card.

Diagrams (3 excalidraw scenes rendered to PNG at 1920x1080 via render.html in the deepsurge profile with serve.py on 8099, exactly as the reference did): problem.png (the named human and the gap), pipeline.png (data sources -> derived index -> tools per role), two-sessions.png (owner window vs partner window, tool lists, server gate in the middle). Palette from this repo's DESIGN.md only.

Screen clips: record from the `qa2` Brave profile (native WebMCP, CDP port 9347) with `node /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/assemble/cdp_record.mjs --port 9347 --target <url-substring> --out /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/clips/<beat>.mp4 --seconds N --width 1600 --height 1000`, driving the page with `bh-multi run qa2 "..."` in a second shell while recording. Create a fresh case first via the API (POST https://pill-round.vercel.app/api/case with {"patientLabel":"Dad","patientAge":78,"medications":[...the 11 rows from src/lib/store/demo.ts...]} (write a small python snippet that regex-reads demo.ts); caregiver = ownerKey, pharmacist = partnerKey). Drive tool calls from the page itself so the on-page TOOL LOG and confirm cards show: getTools() returns a Promise; executeTool(toolObj, JSON.stringify(args)); click the CONFIRM button. Open DevTools > Application > WebMCP on camera at least once if you can (cmd+option+i then navigate); if the panel cannot be driven, show the on-page "Tools registered in this window" panel instead and say so in the report. Both role windows must appear. The SIMULATED label is not needed here (no simulated data in these products).

Assembly: write /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/assemble/timeline.tsv (same columns), cues/*.tsv for pointer highlights on diagram beats, then `bash /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/video/assemble/build_all.sh`. It must print PASS for duration <= 180 s and no dead-air > 2 s. Run verify_video.sh (called by build_all) and look at the extracted frames list. Fix and rebuild until clean.

Upload: `deepsurge` profile is signed in to YouTube. Follow /Users/kamal/Desktop/devpost/projects/webmcp/docs/YOUTUBE.md exactly (studio.youtube.com, DOM.setFileInputFiles over CDP, title under 100 chars, description with Live and Code links, Public, not for kids). Title: "Pill Round: a caregiver and a pharmacist, each with an agent on one medication list (WebMCP, openFDA)". Description: two sentences from docs/DEVPOST.md, then Live: https://pill-round.vercel.app, Code (MIT): https://github.com/kamalbuilds/pill-round, "Built for The WebMCP Challenge.", data source line. Wait for processing to finish, open the youtu.be URL in deepsurge and confirm the title renders. Write /Users/kamal/Desktop/devpost/projects/webmcp/pill-round/docs/YOUTUBE.md in the same shape as the reference.

Commit as you go: `git add video/assemble video/diagrams video/clips/clips.txt video/out/*.mp4 docs/YOUTUBE.md` (never -A; clips/*.mp4 frames dirs are large, add only the final out mp4 and clips.txt), push origin main at the end.

Rules: PATH=/Users/kamal/.nvm/versions/node/v24.9.0/bin:$PATH. ffmpeg at /opt/homebrew/opt/ffmpeg-full/bin. Browser only via bh-multi profiles qa2 (recording) and deepsurge (render + YouTube). Never Google Chrome or Playwright. No em dashes. Do not edit product code; if the product blocks a beat, note it and shoot around it. When done, `swarm report` with: final duration, beats built, YouTube URL, what was verified in frames, and any beat you had to shoot around. Then stop.
