You are the REDESIGN agent for "Pill Round" at /Users/kamal/Desktop/devpost/projects/webmcp/pill-round.

Why: the current look is a near-copy of the first entry (Out of Service: warm paper canvas, Archivo, MTA-signage panels) with a teal accent. Judges will see three entries from this family and they must not look like the same product. Give this one its own visual identity, different from both Out of Service (warm paper, Archivo) and Order to Correct (white broadsheet, serif display).

Reference system: docs/DESIGN-SOURCE-supabase.md (Supabase: white canvas, near-black ink #171717, one emerald primary #3ecf8e / deep #24b47e, humanist sans, soft rounded controls, a night canvas #1c1c1c used for contrast bands). Read it once, fully. Adapt, do not copy branding: no Supabase wordmark, no Circular font (use next/font/google: display and body = "Manrope" or "DM Sans"; mono = "JetBrains Mono" for doses, times, set_ids). Domain semantics on white: boxed/contraindicated = #b42318 (out), warning = #b54708 (watch), ok = #067647 (reliable). Primary CTA emerald #24b47e with #171717 text. Hairline #e5e7eb, canvas #ffffff, canvas-soft #fafafa, ink #171717, muted #707070.

Aesthetic target: a modern clinical app, not a printout: a dark #1c1c1c header band with the patient label and the role chip in emerald; white body; medication rows as soft 8px-radius cards with a left status stripe (active emerald, held amber, stopped grey); flags as bordered callouts with the severity word as a pill and the FDA sentence in a quiet mono block; the round card as a clean grid with time-of-day columns and check marks; buttons pill-ish (8px radius), emerald primary, ghost secondary. No gradients, no shadows heavier than 0 1px 2px rgba(0,0,0,.06), no floating tag pills above cards.

Files you own: DESIGN.md, CLAUDE.md, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/app/c/**, src/components/**, README.md (only if a description must change), public/**. Do not touch src/lib/** or evals/** or src/app/api/**.

Steps:
1. Rewrite DESIGN.md from the Supabase source structure with the tokens above (computed contrast ratios for every text token vs its canvas, written in). Name: Pill-Round-design-system. Keep the @media print section for the round card (black on white). Delete the old paper/Archivo system entirely.
2. Rewrite the @theme block in src/app/globals.css to the new tokens (keep CSS variable NAMES where components use them so churn stays small; rename only where the name is now a lie and update usages). Fonts via next/font/google in layout.tsx.
3. Restyle components per the aesthetic target. The header band, status stripes, severity pills, and the round-card grid are the four signature moves; make sure each exists in the DOM.
4. Verify: `npx tsc --noEmit`; `npx next build`; `uicraft read --as "medication round for a family caregiver and a pharmacist, modern clinical app" --dials 2/2/1 --type "Manrope" --surface "#ffffff" --accent "#24b47e" --radius 8 --showpiece none --cwd .`; `uicraft gate --cwd src` exit 0; `npx next dev -p 3112` then `uicraft look --url http://localhost:3112` at 375 and 1440, plus a case page (POST /api/case with DEMO_MEDICATIONS from src/lib/store/demo.ts and open /c/<id>?k=<ownerKey>). Confirm in DOM: header band background rgb(28,28,28), body background rgb(255,255,255), h1 font-family contains Manrope or DM Sans, medication rows have the status stripe element. Kill the dev server.
5. Commit after each step with `git add <your paths>` (never -A), then `git push origin main`, then `vercel deploy --prod --yes` and confirm https://pill-round.vercel.app returns 200 and the new header band colour in the DOM via bh-multi qa profile.

Rules: Next.js 16 (read node_modules/next/dist/docs/ before app-router edits). Use PATH=/Users/kamal/.nvm/versions/node/v24.9.0/bin:$PATH for node commands. Browser only via bh-multi/bhn profiles (qa, deepsurge). No em dashes in any string. Never the phrase "medical advice"; the openFDA disclaimer footer stays. Semantic tokens only in components, no raw hex. Loading/empty/error states stay. When done, `swarm report` with: tokens chosen, files, the three tells fixed, gate/look output, live URL DOM check. Then stop.
